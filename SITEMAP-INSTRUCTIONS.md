# 📋 تعليمات تشغيل مولد Sitemap

## 🚀 الخطوات السريعة

### 1. تثبيت المتطلبات (مرة واحدة فقط)

إذا لم يكن لديك Node.js مثبت، قم بتثبيته من: https://nodejs.org/

ثم في مجلد المشروع، قم بتشغيل:

```bash
npm install @supabase/supabase-js
```

### 2. تشغيل المولد

```bash
node generate-sitemap.js
```

### 3. النتيجة

سيتم إنشاء/تحديث ملف `sitemap.xml` في نفس المجلد.

---

## 📅 متى يجب تشغيل المولد؟

- ✅ بعد إضافة كتب جديدة
- ✅ بعد إضافة أجزاء جديدة
- ✅ بعد إضافة صفحات جديدة
- ✅ مرة واحدة أسبوعياً (موصى به)

---

## 🔄 أتمتة العملية (اختياري)

### على Windows:
أنشئ ملف `update-sitemap.bat`:
```batch
@echo off
cd /d "%~dp0"
node generate-sitemap.js
echo Sitemap updated successfully!
pause
```

### على Linux/Mac:
أنشئ ملف `update-sitemap.sh`:
```bash
#!/bin/bash
cd "$(dirname "$0")"
node generate-sitemap.js
echo "Sitemap updated successfully!"
```

ثم اجعله قابل للتنفيذ:
```bash
chmod +x update-sitemap.sh
```

---

## ⚠️ ملاحظات مهمة

1. **تأكد من اتصال الإنترنت**: السكريبت يحتاج الاتصال بـ Supabase
2. **لا تعدل الملف يدوياً**: استخدم السكريبت دائماً
3. **راجع النتيجة**: افتح `sitemap.xml` للتأكد من صحته

---

## 📤 إرسال Sitemap إلى Google

بعد توليد `sitemap.xml`:

1. اذهب إلى [Google Search Console](https://search.google.com/search-console)
2. اختر موقعك
3. اذهب إلى "Sitemaps" من القائمة الجانبية
4. أدخل: `https://www.najafdesertlibrary.com/sitemap.xml`
5. اضغط "Submit"

---

## 🐛 حل المشاكل

### خطأ: "Cannot find module '@supabase/supabase-js'"
**الحل:** قم بتشغيل `npm install @supabase/supabase-js`

### خطأ: "node is not recognized"
**الحل:** قم بتثبيت Node.js من https://nodejs.org/

### خطأ في الاتصال بـ Supabase
**الحل:** تأكد من:
- اتصال الإنترنت
- صحة بيانات Supabase في `generate-sitemap.js`

---

**تم! 🎉**
