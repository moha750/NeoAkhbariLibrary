# 🧪 اختبار نظام الدعوات

## ✅ خطوات الاختبار

### 1. افتح Console (F12)

اضغط `F12` في المتصفح لفتح Developer Tools

---

### 2. أرسل دعوة جديدة

```
1. سجل دخول كإداري
2. اذهب لتبويب "فريق العمل"
3. أدخل بريد إلكتروني (مثلاً: test@example.com)
4. اختر دور "محرر"
5. اضغط "إرسال الدعوة"
```

---

### 3. تحقق من Console Logs

يجب أن تظهر هذه الرسائل بالترتيب:

```javascript
✅ تم إنشاء الدعوة بنجاح
🔄 محاولة إرسال البريد الإلكتروني...
📧 محاولة إرسال البريد الإلكتروني...
📧 ملاحظة: إرسال البريد يتطلب Edge Function منشورة
📋 رابط الدعوة: http://127.0.0.1:5500/signup.html?token=inv_xxxxx
⚠️ Edge Function غير متوفرة: FunctionsRelayError: ...
⚠️ Edge Function غير منشورة. الوضع اليدوي مُفعّل.
⚠️ فشل إرسال البريد: إرسال البريد يتطلب نشر Edge Function...
📋 تم نسخ الرابط إلى الحافظة
```

---

### 4. تحقق من Alert Message

يجب أن تظهر رسالة:

```
⚠️ تم إنشاء الدعوة بنجاح، لكن إرسال البريد الإلكتروني غير متاح حالياً.

📋 تم نسخ رابط الدعوة. أرسله يدوياً للشخص المدعو:

📧 test@example.com

🔗 http://127.0.0.1:5500/signup.html?token=inv_xxxxx

💡 لتفعيل الإرسال التلقائي، راجع ملف EMAIL_SETUP_GUIDE.md
```

---

### 5. تحقق من الحافظة (Clipboard)

```
1. افتح أي محرر نصوص (Notepad)
2. اضغط Ctrl+V
3. يجب أن يظهر رابط الدعوة:
   http://127.0.0.1:5500/signup.html?token=inv_xxxxx
```

---

### 6. اختبر رابط الدعوة

```
1. انسخ الرابط من الرسالة أو الحافظة
2. افتحه في نافذة جديدة (Incognito/Private)
3. يجب أن تظهر صفحة signup.html
4. البريد الإلكتروني يجب أن يكون معبأ مسبقاً
5. أدخل كلمة مرور
6. اضغط "إنشاء الحساب"
```

---

## ❌ إذا ظهرت أخطاء

### خطأ: "الدعوة غير موجودة"

**السبب:** مشكلة في قاعدة البيانات

**الحل:**
```sql
-- تحقق من جدول invitations في Supabase
SELECT * FROM invitations ORDER BY created_at DESC LIMIT 5;
```

---

### خطأ: "User not allowed"

**السبب:** محاولة استخدام Admin API من المتصفح

**الحل:** ✅ تم حله! الكود الآن يستخدم Edge Function

---

### خطأ: "Cannot read property 'token' of null"

**السبب:** مشكلة في جلب بيانات الدعوة

**الحل:** ✅ تم حله! الكود الآن يتعامل مع هذه الحالة

---

## ✅ النتيجة المتوقعة

### الوضع الحالي (بدون Edge Function):

```
✅ الدعوة تُنشأ في قاعدة البيانات
✅ الرابط يُنسخ تلقائياً
⚠️ رسالة واضحة: "البريد غير متاح، تم نسخ الرابط"
✅ يمكن إرسال الرابط يدوياً
```

### بعد نشر Edge Function:

```
✅ الدعوة تُنشأ في قاعدة البيانات
✅ البريد يُرسل تلقائياً
✅ رسالة: "تم إرسال الدعوة بنجاح"
✅ المدعو يستلم البريد
```

---

## 🔍 تشخيص متقدم

### في Console، نفذ:

```javascript
// اختبر إنشاء دعوة
const result = await api.createInvitation('test@example.com', 'role-id');
console.log('Invitation created:', result);

// اختبر إرسال البريد
const emailResult = await api.sendInvitationEmail(result.data.id);
console.log('Email result:', emailResult);

// تحقق من البيانات
console.log('Success:', emailResult.success);
console.log('Error:', emailResult.error);
console.log('Link:', emailResult.invitationLink);
console.log('Manual Mode:', emailResult.manualMode);
```

---

## 📊 الحالات المتوقعة

| الحالة | success | error | invitationLink | manualMode |
|--------|---------|-------|----------------|------------|
| Edge Function تعمل | `true` | `null` | ✅ | `undefined` |
| Edge Function غير موجودة | `false` | "يتطلب نشر..." | ✅ | `true` |
| خطأ في الدعوة | `false` | "الدعوة غير موجودة" | ✅ | `true` |

---

## 🎯 الخلاصة

### ما يجب أن يحدث الآن:

```
1. إنشاء الدعوة ✅
2. محاولة إرسال البريد
3. فشل (Edge Function غير موجودة)
4. نسخ الرابط تلقائياً ✅
5. رسالة واضحة للمستخدم ✅
6. لا أخطاء في Console ✅
```

---

## 🚀 للحصول على إرسال تلقائي

```bash
supabase functions deploy send-invitation
```

**بعد ذلك:**
- ✅ البريد يُرسل تلقائياً
- ✅ رسالة: "تم إرسال الدعوة بنجاح"

---

**جرب الآن وأخبرني بالنتيجة! 🎉**
