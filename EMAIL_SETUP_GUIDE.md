# 📧 دليل إعداد البريد الإلكتروني للدعوات

## 🎯 نظرة عامة

تم تنفيذ نظام إرسال الدعوات عبر البريد الإلكتروني باستخدام **Supabase Built-in Email**.

---

## ⚙️ خطوات الإعداد

### 1. إعداد SMTP في Supabase Dashboard

#### الخطوة الأولى: الوصول لإعدادات البريد
```
1. افتح Supabase Dashboard
2. اختر مشروعك
3. اذهب إلى: Authentication → Settings → Email
```

#### الخطوة الثانية: تفعيل Custom SMTP (اختياري)

**الخيار الأول: استخدام Supabase Default SMTP (للتجربة)**
```
✅ لا يحتاج إعداد
⚠️ محدود جداً (4 رسائل/ساعة)
⚠️ قد يذهب للبريد المزعج
```

**الخيار الثاني: استخدام SMTP مخصص (موصى به للإنتاج)**
```
يمكنك استخدام:
├── Gmail SMTP
├── SendGrid
├── Mailgun
├── Amazon SES
└── أي خدمة SMTP أخرى
```

---

## 📝 إعداد Gmail SMTP (مثال)

### 1. إنشاء App Password في Gmail

```
1. اذهب إلى: https://myaccount.google.com/security
2. فعّل "2-Step Verification" إذا لم يكن مفعلاً
3. اذهب إلى: "App passwords"
4. اختر:
   - App: Mail
   - Device: Other (Custom name) → "Supabase"
5. انسخ كلمة المرور المُنشأة (16 حرف)
```

### 2. إعداد SMTP في Supabase

```
في Supabase Dashboard → Authentication → Settings → SMTP Settings:

┌─────────────────────────────────────────────┐
│ Enable Custom SMTP: ✅                      │
│                                             │
│ Sender email: your-email@gmail.com         │
│ Sender name: المكتبة الرقمية               │
│                                             │
│ Host: smtp.gmail.com                       │
│ Port: 587                                  │
│ Username: your-email@gmail.com             │
│ Password: [App Password من الخطوة 1]       │
│                                             │
│ [Save]                                     │
└─────────────────────────────────────────────┘
```

---

## 🎨 تخصيص قالب البريد

### تعديل القالب في Supabase

```
Authentication → Email Templates → Invite user
```

يمكنك استخدام المتغيرات التالية:
```
{{ .ConfirmationURL }}  - رابط التأكيد
{{ .Token }}            - رمز الدعوة
{{ .SiteURL }}          - رابط الموقع
{{ .Email }}            - بريد المستخدم
```

### القالب الحالي (في الكود)

القالب موجود في `supabase-api.js` - دالة `createInvitationEmailTemplate()`:
- تصميم عربي RTL
- ألوان متناسقة
- زر دعوة واضح
- معلومات الدعوة كاملة

---

## 🔧 كيفية الاستخدام

### من لوحة التحكم

```
1. سجل دخول كإداري
2. اذهب لتبويب "فريق العمل"
3. أدخل البريد الإلكتروني
4. اختر الدور (إداري/محرر)
5. اضغط "إرسال الدعوة"
```

### ما يحدث:

```
✅ السيناريو الناجح:
   ↓
   إنشاء الدعوة في DB
   ↓
   إرسال البريد عبر Supabase
   ↓
   رسالة: "✅ تم إرسال الدعوة بنجاح إلى user@example.com"

⚠️ السيناريو البديل (فشل البريد):
   ↓
   إنشاء الدعوة في DB
   ↓
   فشل إرسال البريد
   ↓
   نسخ الرابط تلقائياً
   ↓
   رسالة: "⚠️ تم إنشاء الدعوة ولكن فشل إرسال البريد"
   ↓
   يمكن إرسال الرابط يدوياً
```

---

## 📧 شكل البريد الذي سيصل

```
┌─────────────────────────────────────────────────────────────┐
│  From: المكتبة الرقمية <noreply@yourdomain.com>            │
│  To: user@example.com                                       │
│  Subject: دعوة للانضمام إلى فريق المكتبة الرقمية           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │           🏛️ المكتبة الرقمية                          │ │
│  │           دعوة للانضمام إلى الفريق                    │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                             │
│  مرحباً!                                                   │
│                                                             │
│  تمت دعوتك للانضمام إلى فريق المكتبة الرقمية بصفة: محرر   │
│                                                             │
│  دعوة من: أحمد المدير                                      │
│  البريد الإلكتروني: user@example.com                       │
│                                                             │
│           ┌─────────────────────────┐                      │
│           │     إنشاء الحساب        │  ← زر                │
│           └─────────────────────────┘                      │
│                                                             │
│  ملاحظة: صالحة حتى 17 ديسمبر 2025                          │
└─────────────────────────────────────────────────────────────┘
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: "فشل إرسال البريد"

**الأسباب المحتملة:**

1. **SMTP غير مُعد:**
   - ✅ تحقق من إعدادات SMTP في Supabase
   - ✅ تأكد من صحة Username/Password

2. **حد الإرسال:**
   - ⚠️ Supabase Default: 4 رسائل/ساعة
   - ✅ استخدم SMTP مخصص لإرسال أكثر

3. **البريد يذهب للمزعج:**
   - ✅ استخدم نطاق مُتحقق
   - ✅ أضف SPF/DKIM Records
   - ✅ استخدم خدمة SMTP موثوقة

4. **خطأ في الصلاحيات:**
   - ✅ تحقق من أن المستخدم إداري
   - ✅ راجع RLS Policies

### الحل البديل (دائماً يعمل):

```
إذا فشل البريد:
↓
النظام ينسخ الرابط تلقائياً
↓
أرسله يدوياً عبر:
├── WhatsApp
├── Telegram
├── أي وسيلة أخرى
```

---

## 📊 المقارنة بين الخيارات

| الميزة | Supabase Default | Custom SMTP | Resend/SendGrid |
|--------|------------------|-------------|-----------------|
| الإعداد | سهل جداً | متوسط | متوسط |
| التكلفة | مجاني | مجاني/مدفوع | مجاني ثم مدفوع |
| الحد اليومي | 4/ساعة | حسب الخدمة | 100-1000/يوم |
| الموثوقية | منخفضة | عالية | عالية جداً |
| التخصيص | محدود | كامل | كامل |
| للإنتاج | ❌ | ✅ | ✅ |

---

## ✅ التوصيات

### للتطوير والتجربة:
```
✅ استخدم Supabase Default SMTP
✅ أو نسخ الرابط يدوياً
```

### للإنتاج:
```
✅ استخدم Gmail SMTP (مجاني، 500 بريد/يوم)
✅ أو SendGrid (مجاني، 100 بريد/يوم)
✅ أو Resend (مجاني، 100 بريد/يوم)
```

---

## 🔐 الأمان

### نصائح مهمة:

1. **لا تشارك App Password:**
   - احفظها في Supabase Secrets فقط
   - لا تضعها في الكود

2. **استخدم نطاق مُتحقق:**
   - يمنع البريد من الذهاب للمزعج
   - يزيد الموثوقية

3. **راقب الإرسال:**
   - تحقق من Logs في Supabase
   - راقب معدل الإرسال

---

## 📞 الدعم

إذا واجهت مشاكل:

1. **تحقق من Console:**
   ```javascript
   // افتح Console في المتصفح (F12)
   // ابحث عن أخطاء في إرسال البريد
   ```

2. **تحقق من Supabase Logs:**
   ```
   Supabase Dashboard → Logs → Auth Logs
   ```

3. **اختبر SMTP:**
   ```
   استخدم أداة مثل:
   https://www.smtper.net/
   ```

---

## 🎉 الخلاصة

✅ النظام يعمل بطريقتين:
1. **إرسال تلقائي** عبر البريد (إذا كان SMTP مُعد)
2. **نسخ يدوي** للرابط (كبديل دائماً)

✅ المستخدم لن يتأثر في كلا الحالتين!

---

**تم التنفيذ بنجاح! 🎊**
