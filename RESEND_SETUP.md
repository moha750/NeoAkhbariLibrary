# 🚀 إعداد Resend لإرسال البريد الإلكتروني

## 🎯 المشكلة الحالية

```
✅ Edge Function منشورة وتعمل
❌ لكن RESEND_API_KEY غير مُعد
❌ لذلك البريد لا يُرسل فعلياً
```

---

## ⚡ الحل السريع (5 دقائق)

### الخطوة 1: إنشاء حساب Resend

```
1. اذهب إلى: https://resend.com
2. اضغط "Sign Up"
3. سجل بالبريد الإلكتروني
4. تحقق من بريدك وفعّل الحساب
```

**مجاني:** 100 بريد/يوم، 3000 بريد/شهر

---

### الخطوة 2: الحصول على API Key

```
1. سجل دخول في Resend
2. اذهب إلى: API Keys
3. اضغط "Create API Key"
4. اسم المفتاح: "Supabase Invitations"
5. الصلاحيات: "Sending access"
6. اضغط "Create"
7. انسخ المفتاح (يبدأ بـ re_...)
```

⚠️ **مهم:** انسخ المفتاح الآن! لن تتمكن من رؤيته مرة أخرى.

---

### الخطوة 3: إعداد النطاق (Domain)

#### الخيار 1: استخدام نطاق Resend للتجربة (سريع)

```
✅ لا يحتاج إعداد
✅ يعمل فوراً
⚠️ البريد من: onboarding@resend.dev
```

#### الخيار 2: استخدام نطاقك الخاص (موصى به للإنتاج)

```
1. في Resend Dashboard → Domains
2. اضغط "Add Domain"
3. أدخل نطاقك: yourdomain.com
4. أضف DNS Records في مزود النطاق:
   - SPF Record
   - DKIM Record
5. انتظر التحقق (5-30 دقيقة)
```

---

### الخطوة 4: إضافة API Key في Supabase

```bash
# في Terminal/Command Prompt

# 1. تسجيل الدخول (إذا لم تكن مسجلاً)
supabase login

# 2. ربط المشروع (إذا لم يكن مربوطاً)
supabase link --project-ref yfudytvojcusgemyager

# 3. إضافة RESEND_API_KEY
supabase secrets set RESEND_API_KEY=re_your_api_key_here

# 4. إضافة FROM_EMAIL (اختياري)
supabase secrets set FROM_EMAIL=noreply@yourdomain.com

# 5. إضافة SITE_URL (اختياري)
supabase secrets set SITE_URL=http://127.0.0.1:5500
```

**استبدل:**
- `re_your_api_key_here` بالمفتاح الذي نسخته
- `noreply@yourdomain.com` ببريدك أو استخدم `onboarding@resend.dev`
- `http://127.0.0.1:5500` برابط موقعك

---

### الخطوة 5: إعادة نشر Edge Function

```bash
# إعادة نشر Edge Function لتحميل المتغيرات الجديدة
supabase functions deploy send-invitation
```

---

## ✅ اختبار

### 1. أرسل دعوة جديدة

```
1. افتح dashboard.html
2. تبويب "فريق العمل"
3. أدخل بريد إلكتروني
4. اضغط "إرسال الدعوة"
```

### 2. تحقق من Console

يجب أن تظهر:

```
✅ تم إنشاء الدعوة بنجاح
🔄 محاولة إرسال البريد الإلكتروني...
📧 محاولة إرسال البريد الإلكتروني...
📋 رابط الدعوة: http://...
✅ تم إرسال البريد بنجاح عبر Edge Function  ← هذه الرسالة
```

### 3. تحقق من البريد

```
1. افتح صندوق الوارد
2. ابحث عن بريد من Resend
3. قد يكون في البريد المزعج (أول مرة)
```

---

## 🐛 استكشاف الأخطاء

### "RESEND_API_KEY غير متوفر"

**الحل:**
```bash
# تحقق من المتغيرات
supabase secrets list

# إذا لم يظهر RESEND_API_KEY، أضفه:
supabase secrets set RESEND_API_KEY=re_xxxxx

# أعد نشر Edge Function
supabase functions deploy send-invitation
```

---

### "Invalid API Key"

**الحل:**
```
1. تحقق من أن المفتاح صحيح
2. تأكد من أنه يبدأ بـ re_
3. أعد إنشاء مفتاح جديد في Resend
```

---

### "Domain not verified"

**الحل:**
```
الخيار 1: استخدم onboarding@resend.dev مؤقتاً
الخيار 2: تحقق من DNS Records في نطاقك
```

---

### البريد يذهب للمزعج

**الحل:**
```
1. استخدم نطاق مُتحقق
2. أضف SPF/DKIM Records
3. أرسل بعض الرسائل لبناء السمعة
4. اطلب من المستلمين وضع البريد في "ليس مزعج"
```

---

## 📊 بعد الإعداد

### ما سيحدث:

```
1. الإداري يرسل دعوة
   ↓
2. ✅ تُنشأ في قاعدة البيانات
   ↓
3. ✅ Edge Function تُستدعى
   ↓
4. ✅ Resend يُرسل البريد
   ↓
5. ✅ المدعو يستلم البريد
   ↓
6. ✅ رسالة: "تم إرسال الدعوة بنجاح"
```

---

## 💰 التكلفة

### Resend Free Plan:

```
✅ 100 بريد/يوم
✅ 3,000 بريد/شهر
✅ مجاني للأبد
✅ كافي لمعظم الحالات
```

### إذا احتجت أكثر:

```
Pro Plan: $20/شهر
├── 50,000 بريد/شهر
├── نطاقات مخصصة
└── دعم أولوية
```

---

## 🎯 الخلاصة

### قبل الإعداد:
```
⚠️ "تم إنشاء الدعوة، لكن البريد غير متاح"
📋 نسخ الرابط يدوياً
```

### بعد الإعداد:
```
✅ "تم إرسال الدعوة بنجاح إلى user@example.com"
📧 البريد يصل تلقائياً
```

---

## 🔗 روابط مفيدة

- Resend Dashboard: https://resend.com/dashboard
- Resend Docs: https://resend.com/docs
- Supabase CLI Docs: https://supabase.com/docs/guides/cli

---

**ابدأ الآن! 🚀**
