# 📚 المكتبة الرقمية - نظام إدارة الكتب

مكتبة رقمية متكاملة مع قاعدة بيانات Supabase سحابية، تدعم إدارة الكتب والأقسام والبحث المتقدم.

---

## ✨ المميزات

### 🎯 للقراء
- ✅ تصفح الكتب حسب الأقسام
- ✅ قراءة الكتب بواجهة احترافية
- ✅ البحث في محتوى الكتب
- ✅ تحكم بحجم الخط
- ✅ الوضع الليلي
- ✅ التنقل بلوحة المفاتيح
- ✅ نظام بحث متقدم مع تسليط الضوء

### 👨‍💼 للإدارة
- ✅ إضافة وتعديل الأقسام
- ✅ إضافة وتعديل الكتب
- ✅ رفع صور الأغلفة
- ✅ إدارة الصفحات
- ✅ استقبال رسائل التواصل

---

## 🚀 التثبيت والإعداد

### المتطلبات
- حساب [Supabase](https://supabase.com) (مجاني)
- متصفح ويب حديث
- خادم ويب (للتطوير المحلي)

### خطوات الإعداد

#### 1️⃣ إعداد Supabase

**أ. إنشاء المشروع:**
1. سجل دخول إلى [Supabase](https://supabase.com)
2. أنشئ مشروع جديد
3. انتظر حتى يكتمل الإعداد

**ب. إعداد قاعدة البيانات:**
1. افتح **SQL Editor**
2. انسخ محتوى `database-setup.sql`
3. الصقه ونفذه (Run)
4. إذا واجهت مشكلة RLS، نفذ `fix-rls-policies.sql`

**ج. الحصول على المفاتيح:**
1. اذهب إلى **Settings** > **API**
2. انسخ:
   - **Project URL**
   - **anon public key**

#### 2️⃣ تكوين المشروع

افتح `supabase-config.js` وحدث:

```javascript
const SUPABASE_CONFIG = {
    url: 'YOUR_PROJECT_URL',      // ضع URL هنا
    anonKey: 'YOUR_ANON_KEY',     // ضع المفتاح هنا
    // ...
};
```

#### 3️⃣ تشغيل المشروع

**للتطوير المحلي:**
```bash
# استخدم أي خادم ويب، مثل:
python -m http.server 8000
# أو
npx serve
```

**للإنتاج:**
- ارفع الملفات إلى أي استضافة (Netlify, Vercel, GitHub Pages)

---

## 📁 هيكل المشروع

```
books/
├── index.html              # الصفحة الرئيسية
├── category.html           # صفحة القسم
├── read.html              # صفحة القراءة
├── admin.html             # لوحة التحكم (اختياري)
├── manage-book.html       # إدارة الكتب (اختياري)
├── style.css              # ملف الأنماط
├── supabase-config.js     # إعدادات Supabase ⚙️
├── supabase-api.js        # دوال API
├── database-setup.sql     # إعداد قاعدة البيانات
├── fix-rls-policies.sql   # إصلاح سياسات الأمان
└── README.md              # هذا الملف
```

---

## 🎨 الاستخدام

### للقراء

**1. تصفح الكتب:**
- افتح `index.html`
- تصفح الأقسام والكتب
- استخدم البحث للعثور على كتاب

**2. قراءة كتاب:**
- اضغط على "قراءة الكتاب"
- استخدم الأسهم للتنقل
- اضغط على أيقونة البحث للبحث في المحتوى

**3. التحكم بالقراءة:**
- `+` / `-` : تكبير/تصغير الخط
- `🌙` : تفعيل الوضع الليلي
- `←` `→` : التنقل بين الصفحات

### للإدارة

**إضافة قسم جديد:**
```javascript
const category = await api.addCategory('اسم القسم', 'الوصف');
```

**إضافة كتاب:**
```javascript
const book = await api.addBook({
    title: 'عنوان الكتاب',
    category_id: 'معرف القسم',
    published: true
});
```

**إضافة صفحة:**
```javascript
await api.addPage(bookId, pageNumber, 'محتوى الصفحة');
```

---

## 🔧 API المتاحة

### الأقسام
```javascript
api.getCategories()                    // جلب جميع الأقسام
api.getCategory(id)                    // جلب قسم واحد
api.addCategory(name, description)     // إضافة قسم
api.updateCategory(id, updates)        // تحديث قسم
api.deleteCategory(id)                 // حذف قسم
```

### الكتب
```javascript
api.getPublishedBooks()                // جلب الكتب المنشورة
api.getBooksByCategory(categoryId)     // كتب قسم معين
api.getBook(id)                        // جلب كتاب واحد
api.addBook(bookData)                  // إضافة كتاب
api.updateBook(id, updates)            // تحديث كتاب
api.deleteBook(id)                     // حذف كتاب
api.searchBooks(searchTerm)            // البحث في الكتب
api.incrementBookViews(id)             // زيادة المشاهدات
```

### الصفحات
```javascript
api.getBookPages(bookId)               // جلب صفحات كتاب
api.addPage(bookId, pageNum, content)  // إضافة صفحة
api.updatePage(id, content)            // تحديث صفحة
api.deletePage(id)                     // حذف صفحة
```

### رسائل التواصل
```javascript
api.addContactMessage(messageData)     // إضافة رسالة
api.getContactMessages()               // جلب الرسائل
```

---

## 🔒 الأمان

### Row Level Security (RLS)

تم تفعيل RLS على جميع الجداول مع السياسات التالية:

- ✅ **القراءة**: مفتوحة للجميع
- ✅ **الكتابة**: مفتوحة (يمكن تقييدها لاحقاً)

### تقييد الصلاحيات (للإنتاج)

لتقييد عمليات الإضافة/التعديل/الحذف:

1. فعّل Authentication في Supabase
2. أنشئ مستخدمين إداريين
3. عدّل السياسات في `database-setup.sql`

---

## 🐛 حل المشاكل

### المشكلة: لا تظهر البيانات
**الحل:**
1. افتح Console (F12)
2. تحقق من الأخطاء
3. تأكد من صحة URL و API Key
4. تأكد من تنفيذ `database-setup.sql`

### المشكلة: خطأ RLS
**الحل:**
- نفذ `fix-rls-policies.sql` في SQL Editor

### المشكلة: لا يعمل البحث
**الحل:**
- تأكد من وجود محتوى في الصفحات
- تحقق من Console للأخطاء

---

## 📊 قاعدة البيانات

### الجداول

**categories** (الأقسام)
- `id` - UUID
- `name` - TEXT
- `description` - TEXT
- `created_at` - TIMESTAMP
- `updated_at` - TIMESTAMP

**books** (الكتب)
- `id` - UUID
- `title` - TEXT
- `category_id` - UUID
- `cover_image` - TEXT
- `published` - BOOLEAN
- `views` - INTEGER
- `created_at` - TIMESTAMP
- `updated_at` - TIMESTAMP

**pages** (الصفحات)
- `id` - UUID
- `book_id` - UUID
- `page_number` - INTEGER
- `content` - TEXT
- `created_at` - TIMESTAMP
- `updated_at` - TIMESTAMP

**contact_messages** (رسائل التواصل)
- `id` - UUID
- `name` - TEXT
- `email` - TEXT
- `subject` - TEXT
- `message` - TEXT
- `read` - BOOLEAN
- `created_at` - TIMESTAMP

---

## 🎯 خارطة الطريق

- [ ] نظام تسجيل الدخول للإدارة
- [ ] رفع الصور للأغلفة
- [ ] تصدير الكتب PDF
- [ ] نظام التعليقات
- [ ] الإحصائيات المتقدمة
- [ ] دعم اللغات المتعددة

---

## 📚 موارد إضافية

- [توثيق Supabase](https://supabase.com/docs)
- [Font Awesome Icons](https://fontawesome.com/icons)
- [دليل الإعداد السريع](QUICK_START.md)
- [دليل إصلاح RLS](FIX_RLS_ERROR.md)

---

## 📝 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

---

## 💬 الدعم

للأسئلة والمساعدة:
- افتح Issue على GitHub
- راجع ملفات التوثيق
- تحقق من Console للأخطاء

---

**تم بناؤه بـ ❤️ باستخدام Supabase و Font Awesome**
