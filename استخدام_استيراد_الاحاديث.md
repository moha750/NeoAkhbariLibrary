# دليل استيراد بيانات الأحاديث

## 📋 نظرة عامة

تم إنشاء أداة لاستيراد بيانات الأحاديث من ملفات JSON إلى قاعدة البيانات. الأداة تستخرج **اسم الكتاب** و**رقم الصفحة** فقط من ملفات JSON.

## 🎯 ما تفعله الأداة

### البيانات المستخرجة من كل ملف JSON:
1. **اسم الكتاب** (`bookTitle` أو `bookTitleShort`)
2. **رقم الصفحة** (`pageNum`)
3. **نص الأحاديث** (`cleaned_text`)
4. **معلومات القائل** (`qaelTitleList`) - اختياري

### العملية:
1. تقرأ ملفات JSON من مجلد `الاحاديث_في_صفحات_منفصلة`
2. تستخرج اسم الكتاب من أول حديث في الملف
3. تبحث عن الكتاب في قاعدة البيانات
4. إذا لم يكن موجوداً، تنشئ كتاب جديد
5. تضيف صفحة جديدة تحتوي على جميع الأحاديث من الملف

## 🚀 كيفية الاستخدام

### الخطوة 1: فتح صفحة الاستيراد
افتح الملف: `import-hadiths.html` في المتصفح

### الخطوة 2: اختيار القسم
اختر القسم الذي تريد إضافة الكتاب إليه (مثل: كتب الحديث، الفقه، إلخ)

### الخطوة 3: اختيار الملفات
- اضغط على زر "اضغط لاختيار الملفات"
- اختر ملفات JSON من مجلد `الاحاديث_في_صفحات_منفصلة`
- يمكنك اختيار ملف واحد أو عدة ملفات معاً

### الخطوة 4: بدء الاستيراد
اضغط على زر "بدء الاستيراد" وانتظر حتى تكتمل العملية

## 📊 معلومات الاستيراد

### ما يتم عرضه أثناء الاستيراد:
- **شريط التقدم**: يوضح نسبة الإنجاز
- **ملفات معالجة**: عدد الملفات التي تمت معالجتها
- **إجمالي الأحاديث**: عدد الأحاديث المستوردة
- **صفحات مستوردة**: عدد الصفحات المضافة
- **أخطاء**: عدد الأخطاء إن وجدت

### سجل العمليات:
يعرض تفاصيل كل عملية:
- ✓ نجاح العملية (أخضر)
- ⓘ معلومات (أزرق)
- ✗ خطأ (أحمر)

## 📁 بنية البيانات المستوردة

### في قاعدة البيانات:

#### جدول الكتب (`books`):
```
- id: معرف فريد
- title: اسم الكتاب (من bookTitle)
- category_id: القسم المختار
- published: false (غير منشور افتراضياً)
```

#### جدول الصفحات (`pages`):
```
- id: معرف فريد
- book_id: معرف الكتاب
- page_number: رقم الصفحة (من pageNum)
- content: نصوص الأحاديث مجمعة
```

### تنسيق المحتوى في الصفحة:
```
--- حديث 1 ---
[نص الحديث]
[القائل: امام باقر عليه السلام]

--- حديث 2 ---
[نص الحديث]
[القائل: ...]
```

## ⚠️ ملاحظات مهمة

1. **الكتب المكررة**: إذا كان الكتاب موجوداً بنفس الاسم، لن يتم إنشاء كتاب جديد
2. **الصفحات المكررة**: يمكن إضافة نفس رقم الصفحة أكثر من مرة (تحقق من ذلك يدوياً)
3. **النشر**: الكتب المستوردة تكون **غير منشورة** افتراضياً، يجب نشرها من لوحة التحكم
4. **القسم**: تأكد من اختيار القسم الصحيح قبل الاستيراد

## 🔄 بعد الاستيراد

### في لوحة التحكم (`dashboard.html`):
1. انتقل إلى تبويب "الكتب"
2. ستجد الكتاب المستورد في القائمة
3. يمكنك:
   - تعديل معلومات الكتاب
   - إضافة صورة الغلاف
   - نشر الكتاب
   - إدارة الصفحات

### في تبويب "الصفحات":
1. اختر الكتاب من القائمة
2. ستجد جميع الصفحات المستوردة
3. يمكنك تعديل أو حذف أي صفحة

## 🎨 مثال على الاستخدام

### ملفاتك الحالية:
```
الاحاديث_في_صفحات_منفصلة/
├── page_1.json  (الكافي - صفحة 10)
├── page_2.json  (الكافي - صفحة 11)
├── page_3.json  (الكافي - صفحة 12)
...
```

### بعد الاستيراد:
- **كتاب واحد**: "الکافي"
- **10 صفحات**: من page_1 إلى page_10
- **كل صفحة تحتوي**: على جميع الأحاديث من ملف JSON المقابل

## 🛠️ استكشاف الأخطاء

### "لم يتم العثور على اسم الكتاب"
- تأكد من أن ملف JSON يحتوي على حقل `bookTitle` أو `bookTitleShort`

### "فشل إنشاء الكتاب"
- تحقق من اتصالك بقاعدة البيانات
- تأكد من صلاحيات المستخدم

### "خطأ في معالجة الملف"
- تأكد من أن الملف بصيغة JSON صحيحة
- تحقق من أن الملف يحتوي على مصفوفة من الأحاديث

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تحقق من سجل الأخطاء في صفحة الاستيراد
2. افتح console المتصفح (F12) لمزيد من التفاصيل
3. تأكد من أن ملفات JSON بالتنسيق الصحيح
