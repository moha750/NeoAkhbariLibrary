# 🚀 دليل البدء السريع - Supabase

## الخطوات الأساسية (5 دقائق)

### 1️⃣ إنشاء حساب Supabase
- اذهب إلى: https://supabase.com
- سجل حساب جديد (مجاني)
- أنشئ مشروع جديد

### 2️⃣ إعداد قاعدة البيانات
1. افتح **SQL Editor** في Supabase
2. انسخ محتوى ملف `database-setup.sql`
3. الصقه في SQL Editor
4. اضغط **Run** (Ctrl + Enter)

### 3️⃣ الحصول على المفاتيح
1. اذهب إلى **Settings** > **API**
2. انسخ:
   - **Project URL**
   - **anon public key**

### 4️⃣ تكوين المشروع
افتح `supabase-config.js` واستبدل:
```javascript
url: 'YOUR_SUPABASE_URL',        // ضع Project URL
anonKey: 'YOUR_SUPABASE_ANON_KEY' // ضع anon key
```

### 5️⃣ تفعيل Supabase
في `index.html`، تأكد من:
```javascript
const USE_SUPABASE = true; // ✅ مفعّل
```

---

## ✅ اختبار الاتصال

1. افتح `index.html` في المتصفح
2. افتح Console (F12)
3. يجب أن ترى: `✅ تم الاتصال بـ Supabase`

---

## 📦 نقل البيانات القديمة (اختياري)

إذا كان لديك بيانات في LocalStorage:

1. افتح `migrate-to-supabase.html`
2. اضغط **بدء النقل**
3. انتظر حتى تنتهي العملية

---

## 🎯 الملفات المهمة

| الملف | الوصف |
|------|-------|
| `supabase-config.js` | إعدادات الاتصال |
| `supabase-api.js` | دوال التعامل مع البيانات |
| `database-setup.sql` | إعداد قاعدة البيانات |
| `SUPABASE_SETUP.md` | دليل مفصل |
| `migrate-to-supabase.html` | أداة نقل البيانات |

---

## 🆘 حل المشاكل

**المشكلة**: لا يعمل الاتصال
- ✅ تأكد من نسخ URL و Key بشكل صحيح
- ✅ تأكد من تنفيذ `database-setup.sql`
- ✅ افتح Console وتحقق من الأخطاء

**المشكلة**: لا تظهر البيانات
- ✅ تأكد من `USE_SUPABASE = true`
- ✅ تحقق من وجود بيانات في Supabase (Table Editor)

---

## 📚 موارد إضافية

- 📖 [دليل مفصل](SUPABASE_SETUP.md)
- 🌐 [توثيق Supabase](https://supabase.com/docs)
- 💬 [مجتمع Supabase](https://github.com/supabase/supabase/discussions)

---

**تم! 🎉 الآن مكتبتك متصلة بقاعدة بيانات سحابية احترافية**
